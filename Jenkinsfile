library 'n2p@dev'
properties([
  parameters([
    string(
        name: 'FOLDER_TO_DEPLOY',
        defaultValue: '',
        description: 'Select folder to deploy, otherwise changes in last commit will be deployed', )
   ])
])
deployToTerraLive()
